# jQuery

- What is jQuery?
- What are the features of jQuery?
- Why is jQuery used?
- Differentiate between JavaScript and jQuery.
- How to include jQuery in a webpage?
- What is `$` in jQuery?
- What is $(document).ready()?
- What are jQuery selectors?
- How do you select an element by id? by class?
- How to handle click events in jQuery?
- Differentiate between `.html()` and `.text()`.
- How to hide and show elements using jQuery?
- What are jQuery effects?
- Differentiate between `.hide()` and `.fadeOut()`.
- What is `.slideUp()` and `.slideDown()`?
- How to make an AJAX call using jQuery?
- What is chaining in jQuery?
- How to traverse DOM elements using jQuery?
- Difference between `.click()` and `.on()` methods?
- How to prevent default action of an event in jQuery?
- How do you change CSS using jQuery?
- How do you add and remove classes in jQuery?
- What is AJAX?
- How do you make an AJAX call using jQuery?
- Difference between AJAX and normal HTTP request?
- How to handle form submissions using jQuery?

## Task

Button click → hide/show div

Change text on button click

Form validation (empty check)

Add/remove class dynamically

Load data using AJAX (basic)

## Checklist for readyness

- Step 1: Explain
  - What is jQuery and why is it used?
  - Difference between JavaScript and jQuery
  - What is $?
  - What is $(document).ready()?

- Step 2: Write basic code
  - Select element by id and class
  - Handle click event
  - Hide/show element
  - Change text on button click
  - Add/remove class dynamically

- Step 3: DOM manupulation: Confidence
  - .text() vs .html()
  - .hide() vs .fadeOut()
  - .addClass() vs .css()

- Step 4: On button click, change text color to red and hide a paragraph.

- Step 3: Explain AJAX
  - What is AJAX?
  - Have you used $.ajax() or $.get() or $.post()?
  - What does success callback do?
