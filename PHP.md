# PHP

- What is PHP? and its features?
- Why PHP is used?
- Is PHP a server-side or client-side language?
- Differentiate between PHP and other programming languages.
- How PHP code is executed?
- What is XAMPP / WAMP?
- What is .php file?
- echo vs print in PHP?
- What are variables in PHP?
- What are PHP Data Types?
- empty vs isset in PHP?
- == vs === in PHP?
- control structures in PHP? if, else, switch, for loop, while loop, foreach loop?
- What are arrays in PHP? types of arrays?
- What are associative arrays in PHP?
- What are superglobals in PHP? $_GET, $_POST, $_SESSION, $_COOKIE, $_SERVER, $_REQUEST?
- What are functions in PHP? how to declare a function?
- What is MySQL?
- What is mysqli / PDO?
- Session vs Cookie in PHP?
- What is error handling in PHP?
- GET vs POST methods in PHP?
- PHP thread life cycle?
- Why use sessions and cookies over one another in PHP?
- difference between include and require in PHP?
- indexed arrays vs associative arrays?
- How to solve XAMPP/WAMP server not starting issue?
- SQL Injection? prevention? what are prepared statements? placeholders? in PHP?
- PDO syntax?
- MySQLi syntax?
- static variables in PHP?

## Advanced PHP Questions

- What are traits in PHP?
- What is autoloading in PHP?
